import { Box } from "@mui/material";
import React from "react";

export default function Chatmessages() {
    const arr=localStorage.getItem('messages')
    console.log(arr);
  return (
    <Box>

    </Box>
  )
}
